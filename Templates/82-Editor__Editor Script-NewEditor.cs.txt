using UnityEngine;
using UnityEditor;

[CustomEditor(typeof(SomeType), false, isFallback = fakse)]
public class #SCRIPTNAME# : Editor 
{
    public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();
    }
}